extension Graph.Traversal {
    /// First-visit traversal strategies namespace.
    ///
    /// Contains depth-first and breadth-first traversal implementations
    /// where each node is visited at most once.
    public enum First {}
}
